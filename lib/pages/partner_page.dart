import 'package:flutter/material.dart';

class PartnerPage extends StatelessWidget {
  final String? params;
  const PartnerPage({Key? key, this.params}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Partner page"),
    );
  }
}
